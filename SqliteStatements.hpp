#pragma once
#define CREATE_USERS_TABLE "CREATE TABLE IF NOT EXISTS USERS (USER_ID INTEGER PRIMARY KEY AUTOINCREMENT, USERNAME TEXT NOT NULL UNIQUE, PASSWORD TEXT NOT NULL);"
#define CREATE_TIMERS_TABLE "CREATE TABLE IF NOT EXISTS TIMERS (TIMER_ID INTEGER PRIMARY KEY AUTOINCREMENT, TIMER_NAME TEXT NOT NULL, TIMER_CREATOR TEXT NOT NULL, TIMER_START_TIME INTEGER NOT NULL, TIMER_DURATION INTEGER NOT NULL);"
#define ADD_NEW_USER "INSERT INTO USERS (USERNAME, PASSWORD) VALUES (?, ?);"
#define DELETE_EXISTING_USER "DELETE FROM USERS WHERE USERNAME = ?;"
#define UPDATE_PLAYER_PASSWORD "UPDATE USERS SET PASSWORD = ? WHERE USERNAME = ?;"
#define GET_ALL_USERS "SELECT USERNAME FROM USERS;"
#define VERIFY_USER "SELECT EXISTS(SELECT 1 FROM USERS WHERE USERNAME = ? AND PASSWORD = ? LIMIT 1);"
#define ADD_NEW_TIMER "INSERT INTO TIMERS (TIMER_NAME, TIMER_CREATOR, TIMER_START_TIME, TIMER_DURATION) VALUES (?, ?, ?, ?);"
#define DELETE_EXISTING_TIMER "DELETE FROM TIMERS WHERE TIMER_ID = ?;"
#define GET_ALL_TIMERS "SELECT * FROM TIMERS;"